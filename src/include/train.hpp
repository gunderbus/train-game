#pragma once
#include <string>
#include <vector>

class Train {
public:
    Train(const std::string& name, const std::vector<>)
        : name(name) {};

private:
    std::string name;
    std::vector<std::string> stops;
};